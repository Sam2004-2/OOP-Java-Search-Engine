//package util;

//import javax.swing.*;
//import java.awt.*;

//public class PieChart extends JPanel {

//    private double[] values;
//    private Color[] colors;

//    public PieChart(double[] values, Color[] colors) {
        //this.values = values;
        //this.colors = colors;
    //}

    //@Override
    //protected void paintComponent(Graphics g) {
        //super.paintComponent(g);

        //double total = 0.0;
        //for (double value : values) {
            //total += value;
        //}

        //int startAngle = 0;
        //int arcAngle;
        //for (int i = 0; i < values.length; i++) {
            //arcAngle = (int) Math.round(360 * values[i] / total);
            //g.setColor(colors[i]);
            //g.fillArc(50, 50, 200, 200, startAngle, arcAngle);
            //startAngle += arcAngle;
        //}
    //}

    // Method to update the chart with new data
    //public void updateChart(double[] newValues) {
        //this.values = newValues;
        //repaint();
    //}
//}
